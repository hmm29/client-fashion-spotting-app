/**
 * CatalogView.js
 * See product pic and description
 *
 * @providesModule CatalogView
 * @flow
 */

const React = require('react');
const ReactNative = require('react-native');
const {
  Text,
  View
} = ReactNative;

const ParallaxView = require('react-native-parallax-view');
